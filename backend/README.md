# Arithmetic Merge Backend

To run tests for the backend, run:

```bash
cd backend # if you are in the project root folder
pip install pytest # if you do not have pytest installed
pytest
```

See the [root README](../README.md) for full project documentation.
